% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crc_fit.R
\name{crc_fit}
\alias{crc_fit}
\title{Fit a Capture-Recapture Model}
\usage{
crc_fit(
  data,
  captures,
  capture_formula,
  outcome = NULL,
  outcome_formula = NULL,
  outcome_dist = c("ztnegbin", "ztpois"),
  misclass = NULL,
  latent_classes = 1L,
  control = NULL,
  verbose = FALSE
)
}
\arguments{
\item{data}{A \code{data.frame} or \code{data.table} with one row per observed unit.}

\item{captures}{A one-sided formula identifying binary capture indicators, e.g.,
\code{~ source_1 + source_2 + source_3}.}

\item{capture_formula}{A one-sided formula specifying the log-linear capture model.
The special term \code{.latent} denotes the latent class.}

\item{outcome}{Either \code{NULL}, a single column name for an exactly observed outcome,
or a named character vector \code{c(lower = "lower_column", upper = "upper_column")}.}

\item{outcome_formula}{A one-sided formula for the mean of the outcome distribution.
Must be \code{NULL} when \code{outcome = NULL}.}

\item{outcome_dist}{The zero-truncated outcome distribution.
Currently, \code{"ztnegbin"} and \code{"ztpois"} are supported.}

\item{misclass}{A specification of misclassification mechanisms.}

\item{latent_classes}{A positive integer giving the number of latent classes.}

\item{control}{\code{NULL} or a named list controlling initialization and model fitting.
If \code{NULL}, the default values are used. Supported elements are:
\itemize{
\item \code{init_alpha}: A finite positive number controlling the concentration of the
symmetric Dirichlet distribution used to initialize latent-class weights.
The default is \code{20}.
\item \code{em}: A named list with \code{max_iter}, the maximum number of EM iterations
(default \code{1000L}), and \code{tolerance}, the convergence tolerance (default \code{1e-6}).
\item \code{capture}: A named list with \code{max_iter}, the maximum number of iterations
used in the capture-model M-step (default \code{100L}), and \code{tolerance}, its
convergence tolerance (default \code{1e-8}).
\item \code{outcome}: A named list with \code{max_iter}, the maximum number of iterations
used in the outcome-model M-step (default \code{1000L}), and \code{relative_tolerance},
its relative tolerance (default \code{1e-8}).
}

Unknown control elements are rejected.}

\item{verbose}{A logical value indicating whether progress information should
be displayed during model fitting. The default is \code{FALSE}. Currently unsupported.}
}
\value{
An object of class \code{"crc_fit"}.
}
\description{
Fit a Capture-Recapture Model
}
